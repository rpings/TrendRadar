name: Hot News Crawler

on:
  schedule:
    # 我们使用的是 github 官方提供的资源来进行的推送，而每个账号的资源是限额的，为了不被官方判定为滥用而面临封号的风险，不建议比半小时更低
    # GitHub Actions 使用 UTC 时间，北京时间 = UTC + 8小时
    - cron: "0 0 * * *"  # 北京时间早上8点（UTC 0点）
    - cron: "0 12 * * *" # 北京时间晚上8点（UTC 12点）
  workflow_dispatch:

# 添加权限设置
permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Prepare frequency words configuration
        run: |
          echo "🔍 检查频率词配置..."
          
          # 如果设置了 Secret，优先使用 Secret（保护隐私）
          if [ -n "${{ secrets.FREQUENCY_WORDS_CONTENT }}" ]; then
            echo "✅ 检测到 FREQUENCY_WORDS_CONTENT Secret，将使用 Secret 中的配置"
            echo "从 Secret 读取频率词配置（保护隐私）"
          # 否则检查文件是否存在
          elif [ ! -f config/frequency_words.txt ]; then
            echo "❌ 错误: config/frequency_words.txt 文件不存在，且未设置 FREQUENCY_WORDS_CONTENT Secret"
            echo "请参考项目文档创建频率词配置文件或设置 GitHub Secret"
            exit 1
          else
            echo "✅ 使用文件 config/frequency_words.txt"
          fi

      - name: Prepare platforms configuration
        run: |
          echo "🔍 检查平台配置..."
          
          # 如果设置了 Secret，优先使用 Secret（保护隐私）
          if [ -n "${{ secrets.PLATFORMS_CONTENT }}" ]; then
            echo "✅ 检测到 PLATFORMS_CONTENT Secret，将使用 Secret 中的配置"
            echo "从 Secret 读取平台配置（保护隐私）"
          else
            echo "✅ 使用配置文件 config/config.yaml 中的平台配置"
          fi

      - name: Verify required files
        run: |
          echo "🔍 检查必需的配置文件..."

          if [ ! -f config/config.yaml ]; then
            echo "❌ 错误: config/config.yaml 文件不存在"
            echo "请参考项目文档创建配置文件"
            exit 1
          fi

          echo "✅ 配置文件检查通过"

      - name: Run crawler
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          WEWORK_WEBHOOK_URL: ${{ secrets.WEWORK_WEBHOOK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
          WEWORK_MSG_TYPE: ${{ secrets.WEWORK_MSG_TYPE }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_SERVER_URL: ${{ secrets.NTFY_SERVER_URL }}
          NTFY_TOKEN: ${{ secrets.NTFY_TOKEN }}
          FREQUENCY_WORDS_CONTENT: ${{ secrets.FREQUENCY_WORDS_CONTENT }}
          PLATFORMS_CONTENT: ${{ secrets.PLATFORMS_CONTENT }}
          GITHUB_ACTIONS: true
        run: python main.py

      - name: Commit and push if changes
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add -A
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update by GitHub Actions at $(TZ=Asia/Shanghai date)" && git push)
